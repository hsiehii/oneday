const state = {
  days: null
};
const getters = {
  DAYS: (state) => {
    return state.days;
  }
};
const mutations = {
};

const actions = {
};

export default {
  state,
  getters,
  mutations,
  actions
};
